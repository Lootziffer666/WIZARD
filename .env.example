# Copy to .env for docker compose or local development.
# You only need one provider key; the UI can also store a key locally in your browser.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional: local/remote DB overrides.
DATABASE_PATH=./data/assets.db
DATABASE_URL=
DATABASE_AUTH_TOKEN=
